{
  "name": "liquid-glass-refraction-shader",
  "private": true,
  "version": "1.0.0",
  "description": "Screen-space refraction in WebGL2: simulated index of refraction (IOR), chromatic dispersion, and Fresnel reflections compared against CSS backdrop-filter and SVG feDisplacementMap.",
  "type": "module",
  "license": "MIT",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "format": "prettier --write ."
  },
  "devDependencies": {
    "prettier": "^3.3.0",
    "typescript": "^5.6.0",
    "vite": "^6.0.0",
    "vitest": "^2.1.0"
  }
}
